export const generatePermanentId = (prefix: string, num: number) => {
  // Ensure proper padding to 4 digits
  const paddedNum = num.toString().padStart(4, '0');
  return `${prefix}${paddedNum}`;
};

export const generateSkuId = (title: string) => {
  const code = title.replace(/\s+/g, '-').toUpperCase().slice(0, 6);
  return `${code}-${Date.now().toString().slice(-5)}`;
};

export const generateSixDigitAlphaNumericCode = () => {
  const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';
  let result = '';
  for (let i = 0; i < 6; i++) {
    result += chars.charAt(Math.floor(Math.random() * chars.length));
  }
  return result;
};

export const autoGeneratedPassword = () => {
  const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789#$&@%';
  let result = '';
  for (let i = 0; i < 8; i++) {
    result += chars.charAt(Math.floor(Math.random() * chars.length));
  }
  return result;
};